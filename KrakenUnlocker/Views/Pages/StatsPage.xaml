<Page
    x:Class="KrakenUnlocker.Views.Pages.StatsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:KrakenUnlocker.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Stat Editor"
    x:Name="StatsPageRoot"
    d:DataContext="{d:DesignInstance local:StatsPage, IsDesignTimeCreatable=False}"
    d:DesignHeight="700" d:DesignWidth="900"
    Background="#0A0505"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid Margin="28,16,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Stat Editor" FontSize="26" FontWeight="Bold" Foreground="White"/>
                <TextBlock Text="Load a game's stats by Title ID and edit values directly." 
                           FontSize="12" Foreground="#666" Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- Search Bar -->
        <Border Grid.Row="1" Background="#0D0D0D" BorderBrush="#1A1A1A" BorderThickness="1"
                CornerRadius="10" Padding="20,16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ui:TextBox Grid.Column="0"
                            x:Name="StatTitleIdBox"
                            PlaceholderText="Enter Title ID  e.g. 1234567890"
                            Text="{Binding ViewModel.TitleId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                            FontSize="14" Padding="12,10"/>

                <Button Grid.Column="1" Margin="12,0,0,0" Cursor="Hand"
                        IsEnabled="{Binding ViewModel.NotBusy}"
                        Command="{Binding ViewModel.LoadStatsCommand}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="#CC0000" CornerRadius="6" Padding="20,10">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="14" Foreground="White" Margin="0,0,6,0"/>
                                    <TextBlock Text="Load Stats" Foreground="White" FontWeight="Bold" FontSize="13"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#FF0033"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="bg" Property="Background" Value="#2A2A2A"/>
                                    <Setter TargetName="bg" Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <TextBlock Grid.Column="2" Margin="16,0,0,0" VerticalAlignment="Center"
                           FontSize="12" Foreground="#888" TextTrimming="CharacterEllipsis"
                           Text="{Binding ViewModel.StatusText}"/>
            </Grid>
        </Border>

        <!-- Busy bar -->
        <Border Grid.Row="2" Height="3" CornerRadius="2" Margin="0,0,0,16"
                Background="#1A0505" Visibility="{Binding ViewModel.IsBusy, Converter={StaticResource BoolToVis}}">
            <Border.Effect>
                <DropShadowEffect Color="#FF0033" BlurRadius="12" ShadowDepth="0" Opacity="0.9"/>
            </Border.Effect>
            <ProgressBar IsIndeterminate="True" Height="3" Background="Transparent"
                         Foreground="#CC0000" BorderThickness="0"/>
        </Border>

        <!-- Stats Grid -->
        <Border Grid.Row="3" Background="#0D0D0D" BorderBrush="#1A1A1A" BorderThickness="1" CornerRadius="10">
            <DataGrid AutoGenerateColumns="False"
                      ItemsSource="{Binding ViewModel.Stats}"
                      CanUserAddRows="False" CanUserDeleteRows="False"
                      CanUserReorderColumns="False" CanUserResizeColumns="True"
                      CanUserResizeRows="False" CanUserSortColumns="True"
                      HeadersVisibility="Column"
                      VerticalScrollBarVisibility="Visible"
                      HorizontalScrollBarVisibility="Disabled"
                      Background="Transparent" BorderThickness="0"
                      RowBackground="#0D0D0D" AlternatingRowBackground="#111111"
                      GridLinesVisibility="Horizontal" RowHeight="52" FontSize="12">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#0A0A0A"/>
                        <Setter Property="Foreground" Value="#CC0000"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="FontSize" Value="10"/>
                        <Setter Property="Padding" Value="14,10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#1A1A1A"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="14,8"/>
                        <Setter Property="Foreground" Value="#CCC"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1A0505"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="STAT" Binding="{Binding DisplayName}" Width="0.3*" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="INTERNAL NAME" Binding="{Binding Name}" Width="0.24*" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Opacity" Value="0.5"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                                <Setter Property="FontSize" Value="11"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="TYPE" Binding="{Binding StatType}" Width="0.12*" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#CC0000"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="VALUE" Binding="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="0.2*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="Foreground" Value="#FFD070"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTemplateColumn Header="SAVE" Width="0.1*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Margin="4" HorizontalAlignment="Stretch" Cursor="Hand"
                                        Command="{Binding ViewModel.SaveStatCommand, RelativeSource={RelativeSource AncestorType={x:Type Page}}}"
                                        CommandParameter="{Binding}">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="bg" Background="#1A1A1A" CornerRadius="4" Padding="6,4"
                                                    BorderBrush="#CC0000" BorderThickness="1">
                                                <TextBlock Text="💾" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bg" Property="Background" Value="#CC0000"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="✓" Binding="{Binding Status}" Width="0.04*" IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                                <Setter Property="FontSize" Value="14"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
